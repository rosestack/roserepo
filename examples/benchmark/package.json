{
  "name": "benchmark",
  "type": "module",
  "scripts": {
    "turbo": "turbo run build --no-cache --force",
    "roserepo": "roserepo run build --no-cache",
    "benchmark": "node benchmark.js 6"
  },
  "dependencies": {
    "turbo": "^1.11.1",
    "roserepo": "file:../../packages/roserepo"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ]
}
